name: server-tests
on: 
  - push
  - pull_request
jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: install dependencies
        run: npm i
      - name: run unit tests
        run: npm run test
